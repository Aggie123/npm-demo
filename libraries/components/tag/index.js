'use strict'
import React, {
  Component,
  PropTypes 
} from 'react';
import {
  View,
  Image,
  Text,
  TouchableHighlight,
  StyleSheet,
} from 'react-native';
import defaultStyle from 'mjuistyle/theme/default'
import fontStyle from 'mjuistyle/theme/fontStyle'
import {styles} from './style'

export default class MiTag extends Component {
  constructor(props) {
      super(props);
  }

  static propTypes = {
 
  }

  static defaultProps = {

  }

  render() {
    let { 
 
    } = this.props;

    return (
      <View>
      </View>
    )
  }
}